/* Header corrigé : garde d'inclusion + les deux prototypes. */
#ifndef MODULES2_TABLE_H
#define MODULES2_TABLE_H

#include <stddef.h>

int  max_tableau(const int *tab, size_t n);
long somme_tableau(const int *tab, size_t n);

#endif /* MODULES2_TABLE_H */
